# Godot 4+
.godot/
*.translation

# .NET / Rider / VS / Claude Code
bin/
obj/
.idea/
.vs/
.claude/
*.user
*.suo

# Local-only build overrides
local.props

# Logs / runtime artifacts
*.log

# Bundled helper exe (built/copied separately, not source)
helper/

# Release zips at repo root + staging dir used to build them
/Sts2WinratePreview-v*.zip
/_release_staging/
